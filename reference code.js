//sucsesful filter
var searchTraits = traitArray.filter(function(num) {
  num == data.object[i];
  retun
});
var newList = listOfPeople.filter(function(person){ return person.height === searchCrit[1]});
  return newList;
